# Suivi d’implémentation du sandbox

Plan : `2026-09-10-sandbox-generalisation.md`.

Autorisation : l’utilisateur demande l’implémentation avec un worker principal GPT-5.6 Terra et un quick worker si utile. Garde le contrôleur en coordination. L’implémentation remplace le statut « planification seule » du document initial. UDP reste reporté en A7.

## État vérifié actuel

- Implémentation A1–A6 qualifiée puis installée personnellement sous WSL le 2026-09-11 après autorisation explicite. Les worktrees sont conservés. UDP reste reporté en A7.
- Pi : 626 tests réussis, 5 non exécutés, 2 557 assertions sur 71 fichiers. Ce total rassemble le dernier résultat de chaque fichier exécuté dans un processus Bun distinct. Il ne représente pas une passe groupée unique.
- Zerobox : 169 intégrations réussies avec le CLI release, 4 ignorées. Bibliothèques : 682 réussites, 1 ignorée. Le binaire final porte le SHA-256 `87a73d1bd2556ad629e3a675b8d39898aa693c4e0b7cc27c33078e4a3b298b7f`.
- Provenance, configuration globale/projet, migration, permissions, ressources Unix/TCP, révocation, HOME privé et client Analysis avec IPC par défaut sont vérifiés. Les deux typechecks Pi passent.
- Limites de qualification : Linux natif, interop Windows et accès depuis un autre poste LAN non exécutés. Les chemins de contrôle Unix restent bornés à moins de 108 octets. L’erreur intermittente du lancement groupé Bun et l’échec du lint typé sont détaillés ci-dessous.

## Correction Docker et activation personnelle du 2026-09-11

- Réserve l’autorisation générale Docker, les limites communes d’opérations et les exceptions sensibles exactes au global. Déclare les cibles ordinaires et leurs opérations dans les projets. Aucun registre global de cibles ordinaires ne subsiste. Le break-glass vérifie la politique effective du projet et la persistance de son exception globale après confirmation.
- Les 112 tests ciblés Docker, profils, lifecycle, index, migration et intégrations broker passent. Les tests broker emploient un moteur de fixture, distinct du daemon personnel. Les listes historiques de chemins supplémentaires vides ne deviennent plus des listes actives vides qui fermaient le projet.
- Le premier essai réel personnel a ensuite révélé des entrées `~/…` de `environment.path` conservées telles quelles après composition. RED via le vrai `loadSandboxConfig`, puis correction avec la fonction générique d’expansion existante. Les 19 tests de profils passent, ainsi que 35 tests d’autorité active, migration et compilation de politiques. Ces groupes se recouvrent et ne doivent pas être additionnés.
- Installation : 73 chemins de sources synchronisés avec vérification des fichiers précédents, binaire final remplacé atomiquement, puis migration globale et des projets Cliproxy/Shein. Les archives exactes et le manifeste sont dans `~/.local/state/pi/sandbox-activation-20260911-KKgIngiq`. Les anciennes sources `sandbox.global.json` et `sandbox.capabilities.json` quittent les emplacements actifs. Les `settings.json` sont inchangés.
- Configuration personnelle : mode sandbox, domaines historiques conservés, partage explicite du `/tmp` hôte pour le shell, Think privé, accès aux outils installés par chemins génériques. Docker initialement activé seulement dans Cliproxy, avec les opérations historiques et son exception sensible globale exacte. Les autres projets peuvent choisir leurs propres cibles ordinaires.
- Deux essais personnels réussis, 62 assertions, dans un nouveau processus : sources chargées depuis `~/.pi/agent/extensions`, Pi résolu dans `~/projects/pi-core/packages/coding-agent/dist/index.js`, binaire installé au SHA final. HOME/cache privé utilisable, voisin de fixture inaccessible, Go/Bun/rg disponibles, inspection Cliproxy et sonde exec permise, exec libre refusé, Docker désactivé dans Shein. Les deux projets exécutent réellement JavaScript, Python et une commande Think. Seuls le fournisseur LLM et l’emplacement du magasin Think sont remplacés pour l’essai.
- TypeScript passe après la correction PATH. Oxlint termine désormais mais signale 76 erreurs préexistantes, dont trois hors des lignes modifiées dans `capabilities/policy.ts`. Aucun lint global vert n’est revendiqué. La qualification antérieure de 626 tests n’a pas été entièrement répétée pour cette activation.
- La session Pi préexistante dans Shein est préservée. Relance-la pour charger le nouveau code. Aucun démarrage, arrêt ou redémarrage de service Docker n’a été exécuté. Linux natif, interop Windows et LAN distant restent non qualifiés. Les améliorations génériques supplémentaires des diagnostics issues de l’audit de session restent distinctes de cette activation.

## Changements livrés

| Lot | Résultat |
| --- | --- |
| A1 / A1b | Lecture bornée, cwd canonique, HOME privé, sockets Unix précis et publications TCP explicites. Rejeu des patchs vérifié. Fermeture, drainage, redémarrage et concurrence couverts. |
| A2 | Configuration v2 dans les deux emplacements `sandbox.json`, plafonds globaux et overrides projet, migration explicite avec archives et récupération. Docker exige autorisation globale et activation projet. |
| A3 | Socle de fichiers et outils générique, chemins canoniques et caches privés utilisables, même commande ordinaire sur deux configurations de fixture. |
| A4 | Routes et adaptateurs par produit retirés du contrat actif. Deux modes : `sandbox` et `host`. Profils descriptifs : `default`, `custom`, `host`. Aucun choix de backend par produit ni repli hôte implicite. |
| A5 | Permissions préservées, contrôle des admissions et des changements de configuration, interruption des détenteurs de ressources retirées et expiration des accès Docker temporaires. Think/Analysis restent stricts. |
| A6 | Documentation et schéma v2, provenance exacte, tests de frontière et parcours Pi réels, procédure d’activation séparée. |

Les décisions D1–D12 restent des décisions d’architecture. Elles n’ajoutent aucun profil ni fichier de configuration. Les deux emplacements actifs sont `~/.pi/agent/sandbox.json` et `<projet>/.pi/sandbox.json`.

## Qualification Pi finale

- Le coordinateur a exécuté 69 fichiers, chacun avec `bun test --isolate <fichier>`, dans les cinq répertoires de frontière du plan. Première passe : 611 réussites, 2 échecs de fixture, 5 non exécutés, 2 316 assertions, 108,60 s. Les fixtures en échec plaçaient leur répertoire technique sous `/tmp` et recevaient l’avertissement du CLI release. Elles utilisent maintenant `/var/tmp`, sans supprimer l’avertissement ni affaiblir les assertions de sortie.
- Derniers résultats des deux fichiers corrigés : `runtime/profiles.integration.test.ts` 6 réussites / 31 assertions ; `runtime/shell-baseline.integration.test.ts` 1 réussite / 5 assertions.
- Deux fichiers complémentaires : `dependency-contract.test.ts` 9 réussites / 200 assertions ; `analysis/real-engine.integration.test.ts` 4 réussites / 34 assertions. Le premier compare le binaire et les sources réels au manifeste. Le second exécute JavaScript et Python via le vrai client Analysis et son IPC par défaut, sans injection `runHost`, vérifie une erreur transmise avec provenance et le nettoyage des leases.
- Total calculé à partir du dernier résultat de chacun des 71 fichiers : **626 réussites, 0 échec, 5 non exécutés, 2 557 assertions**. Les cinq non exécutés concernent le scénario réseau local optionnel et quatre commandes de workflow d’un projet externe, dont les variables d’activation ne sont pas définies.
- Environnement commun : Bun 1.3.14, `TMPDIR=/tmp`, `PI_SANDBOX_ZEROBOX_BINARY=/tmp/pi-sandbox-candidate-87a73d/zerobox`, SHA final explicite. Les essais concernés activent les flags `SHELL_BASELINE`, `GENERIC_COMMAND`, `REAL_SHELL_MODES`, `LOCAL_RESOURCES`, `REAL_ANALYSIS` et `REAL_ANALYSIS_IPC` sous le préfixe `PI_SANDBOX_` et le suffixe `_CONTRACT`. Le contrôle de source utilise aussi `PI_SANDBOX_ZEROBOX_SOURCE_ROOT` vers le worktree backend.
- Typechecks : `bun run typecheck` et `./node_modules/.bin/tsc --noEmit -p tsconfig.sandbox.json` réussis. Formatage des fichiers production modifiés et `git diff --check` réussis. Le contrôle lint typé avait rencontré une panne Oxlint/tsgolint ; aucun résultat lint vert n’est revendiqué.

## Limites et activation

- Linux natif, trajet Windows et client LAN distinct nécessitent leurs propres essais. Les deux configurations de V3 sont des fixtures sur WSL, pas deux machines physiques.
- Le test IPC utilise un HOME court créé exclusivement en mode 0700. Le chemin Unix maximal calculé est de 107 octets. Les HOME arbitrairement longs ne sont pas qualifiés et peuvent dépasser le budget de contrôle.
- Une erreur intermittente `EEXIST: epoll_ctl`, suivie d’une erreur Jiti sur `process.stderr.fd`, empêche de revendiquer une qualification Pi groupée reproductible. Les contrôles n’ont pas confirmé de fuite dans les fixtures. Les derniers résultats retenus proviennent de processus distincts.
- Deux échecs backend transitoires ont accompagné un tmpfs de runtime presque plein. La dernière suite complète passe. Aucun nettoyage de répertoires techniques personnels partagés n’a été effectué. Les anciennes fixtures ayant utilisé de tels répertoires restent signalées dans l’historique.
- La méthode RED avant code comporte les exceptions documentées pour A4 et l’extension du scénario TCP multi-générations. Les régressions critiques ultérieures de révocation et d’inscription tardive ont été observées RED puis GREEN.
- L’activation personnelle autorisée est consignée ci-dessus et dans `2026-09-10-sandbox-generalisation-activation.md`. Les worktrees ne sont ni committés ni fusionnés.

Les sections historiques suivantes conservent les résultats intermédiaires et les incidents. Les snapshots c10ad, 0522ff et 241a2f ne sont pas le binaire final.

## Artefact backend final

- Binaire de qualification : `/tmp/pi-sandbox-candidate-87a73d/zerobox`, version `0.3.3-fork.17`, SHA-256 `87a73d1bd2556ad629e3a675b8d39898aa693c4e0b7cc27c33078e4a3b298b7f`. Copie et empreinte vérifiées par le coordinateur.
- Sources : worktree `~/projects/shared-services/sandboxes/zerobox-generalisation`, commit `ebd12774aafa63fec1864e04f248150ec50136d4`, engine `9b8cf56cdefb09f54564ccc295fd42f6647f558f`. Les sources du build local sont maintenant committées, sans prétention de release taguée. Le binaire installé conserve son empreinte.
- Manifeste Pi : `agent/extensions/sandbox/runtime/zerobox-provenance.json` dans le candidat. Il contient les empreintes des 35 patchs présents, leur ordre, et l’omission historique explicite du patch optionnel absent `upstream-no-preemptive-codex-protect.patch`.
- Empreinte des changements source avant commit, depuis la base `5c530891c2883bfbfd192883002cfc9b0f50e632` : `e44193a655ddb491c79f85e88b13d79aedc18ab53531093d51b28765c8be979a`. Algorithme : SHA-256 de `git diff --binary HEAD`, suivi des diffs binaires `/dev/null` des fichiers non suivis triés par chemin. Après commit, le manifeste référence le commit exact et l’empreinte du diff vide `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. Le contrat de provenance compare les fichiers réels, le HEAD, le binaire et le manifeste.
- Rapport terminal backend : rejeu propre sans `.rej`, formatage, check, build release et diff réussis. Bibliothèques : 682 réussites, 1 ignoré. Intégrations avec CLI release explicitement désigné : 169 réussites, 4 ignorés. Les tests SDK restent liés au profil de `cargo test`. TCP : 12 réussites sans verrou externe.
- Incidents de qualification : deux échecs transitoires de runtime lors d’une passe complète, avec tmpfs `/run/user/1000` presque plein et erreur de remount. Les deux cas ciblés puis la suite complète ont réussi. Aucun nettoyage du runtime partagé personnel n’a été effectué.

## Historique de qualification

### Dernier jalon A4 et lancement A3/A5

- A4 corrigé terminé : contrats et adaptateurs produits actifs retirés, backend choisi uniquement par mode, provenance mode/profil distincte des preuves observées. Les anciens résultats integrated ne sont convertis qu’avec une preuve du moteur réellement utilisé.
- Le contrôleur a refusé la suppression des tests réels de protection de l’autorité et de refus de permission. Ils ont été restaurés en v2, y compris les alias symlink/hardlink. Rapport corrigé : 67 réussites, 241 assertions. Contrôle local : 64 réussites avant restauration, puis 5 réussites et 26 assertions sur le fichier restauré. Typecheck et diff annoncés réussis par le worker.
- Limite de méthode A4 : les nouvelles assertions mode/provenance ont été ajoutées après les changements. Le seul RED historique rapporté concernait une ancienne assertion de schéma. Ne présente pas ce lot comme ayant suivi intégralement RED avant code.
- A3/A5 attribué au second worker : validation des sections Docker inactives, broker réel de fixture, plafonds tmp et Think privé, admission user_bash !s, permissions et commandes littérales. Utilise le snapshot c10ad qualifié. Les ressources A1b attendent le backend.
- A1b en cours : premier échange réel réussi sur le socket Unix explicitement autorisé, voisin du projet refusé. Alias, remplacement, tmp partagé, preuve côté voisin et publications restent en qualification. Interface publication retenue : scope explicite host/lan, adresses SocketAddr précises, cible loopback privée, transport TCP. UDP reste refusé explicitement.

### Qualification Docker, HOME et transports

- V2 Docker : le coordinateur a exécuté `docker-policy-config.integration.test.ts` avec le snapshot c10ad. Résultat : 1 réussite, 34 assertions. La même session Pi traverse interdiction globale, absence d'activation projet, activation, révocation globale, réactivation et révocation projet. Les réponses et les compteurs du moteur Docker jetable sont vérifiés. Aucun daemon personnel n'est utilisé.
- V3 : les essais de partage explicite de `/tmp`, de restriction privée par projet et de Think privé passent. Le test du cache révèle une contradiction Pi entre le deny du parent de contrôle et l'allow de la HOME descendante. Conserve la priorité backend des denies. Monte la HOME privée à une destination logique distincte.
- Pré-requis HOME backend : snapshot `/tmp/pi-sandbox-qualified-0522ff/zerobox`, SHA-256 `0522ffb7d47f9ef2d41185bbafb9735b605669619cd84b4f80067d9e59a5c871`, version `0.3.3-fork.17`. Le coordinateur a vérifié le hash. L'intégration Pi et les assertions d'écriture de cache restent à qualifier. Le premier rapport backend annonçait une écriture absente du test lu : ne retiens pas cette preuve avant correction.
- Isolation des fixtures : la création et la récupération des leases utilisent encore la racine personnelle dans certaines anciennes fixtures, malgré un backend et un probe de test. Injecte les deux seams typées vers une racine temporaire. Ne nettoie aucune ressource personnelle. Les preuves fonctionnelles précédentes ne prouvent pas une absence totale d'écritures techniques personnelles.
- A1b Unix : quatre intégrations annoncées vertes couvrent socket exact, voisin/alias, tmp partagé, abstraction privée et remplacement refusé. A1b TCP reste RED : parsing et propagation helper présents, bridge de publication à terminer. La réservation paresseuse après LISTEN doit permettre une commande ordinaire concurrente et isoler le conflit d'un second serveur.
- Écarts A5 en traitement : vérifie la provenance de `!s` pendant une session host et restaure le dispatch break-glass Docker prévu par le plan, avec double porte v2 et expiration interruptive. Ne réintroduis aucun registre global par projet.
- A6 : README et guide de configuration réécrits vers les deux fichiers v2. Schéma, ressources locales, Docker et guides runtime restent à finaliser après stabilisation du code.

### Vérification du correctif HOME par le coordinateur

- Snapshot `0522ff` : `profiles.integration.test.ts` et `docker-policy-config.integration.test.ts` exécutés ensemble, 7 réussites, 67 assertions, 30,47 secondes. Le test écrit les caches XDG/Bun/npm, masque marker et secret de la session voisine avec le parent de contrôle dans le projet, et vérifie l'injection de création/récupération des leases dans une racine temporaire.
- Le compilateur transmet `--private-home` et utilise `/home/sandbox` pour HOME/caches. Les chemins `~/…` de la configuration restent résolus depuis le HOME utilisateur. Les denies qui ne peuvent pas être appliqués à cette HOME logique sont refusés explicitement. Une erreur de calcul du préfixe statique des globs a été signalée au worker : un deny `/tmp/*.pem` ne doit pas être confondu avec un deny de `/`.
- Le schéma `docs/sandbox.schema.json` remplace l'ancien schéma Docker. Validation Draft 2020-12 et fixtures positives/négatives réussies, notamment champs globaux interdits dans le projet. README, configuration, Docker, runtime et dépannage ne prescrivent plus les anciens sélecteurs et fichiers actifs. Les ressources A1b et la commande break-glass attendent leur implémentation finale pour compléter ces guides.
- Le backend TCP est repris par un nouveau worker Terra avec contexte réduit après plusieurs rapports partiels sans réalisation du bridge. Le contrôleur reste en coordination. Aucun succès TCP n'est revendiqué.

### Écarts de revue après le rapport A3/A5

- Le coordinateur a rejoué le socle réel avec fixtures isolées et snapshot0522ff : 1 réussite, 5 assertions. La preuve exacte `./bin/check` sur deux configurations locales est confiée séparément à un worker, dans un seul nouveau fichier de test.
- Le rapport A3/A5 ne clôt pas le break-glass : le premier correctif active un runtime éphémère mais la prochaine admission reconstruit la politique ordinaire. L'expiration utilise encore une configuration capturée, et la confirmation n'est pas suivie d'une relecture des portes. Demande des tests à l'admission, à la révocation pendant confirmation et à l'expiration avant clôture.
- La preuve `!s` reçue utilise le vrai routage Pi avec un service sandbox mocké. Elle ne prouve pas l'isolation Zerobox et ne vérifie initialement pas la provenance. Demande une assertion de provenance et un parcours réel isolé, sans présenter le mock comme une preuve système.
- Le backend a obtenu un replay syntaxique en retirant un hunk Unix nécessaire au refus AF_UNIX non-STREAM. Ce replay n'est pas une validation fonctionnelle. Restaure ce filtre et son test avant de produire un nouveau binaire qualifié. Le bridge TCP reste une ébauche non compilée. Les snapshots c10ad et0522ff conservent leur identité et leurs seules preuves déjà établies.
- La surface Pi proposée pour A1b est validée : `resources.unixSockets` et `resources.tcpPublications`, tuples transport/scope/listen/target, plafond global et restrictions projet/session. L'intégration est en attente des correctifs et de la qualification backend.

### Preuve V3 et restauration du filtre Unix

- V3 reçu, relu et exécuté par le coordinateur : `runtime/generic-command.integration.test.ts`, 1 réussite, 16 assertions, 3,37 secondes sur snapshot0522ff. Les deux projets exécutent exactement `./bin/check` avec scripts identiques, outils extérieurs trouvés via PATH, caches distincts inscriptibles et voisins illisibles/non modifiables. Chaque document global de fixture autorise les chemins des deux projets ; le document projet réduit la liste à ses chemins. Les variables globales propres à chaque fixture restent distinctes. La preuve utilise loader/service/shell réels, sans harness Pi.
- Le worker de cette preuve signale Oxfmt qui exclut les tests et un panic tsgolint lors du lint ciblé. Ne présente pas ces diagnostics comme réussis. Le test fonctionnel et le diff sont indépendants de ce problème d'outillage.
- Le worker backend confirme la restauration du filtre AF_UNIX non-STREAM : le test réel d'abstraction privée et refus SOCK_DGRAM passe (1 test), puis sync complet sans rejet. Le bridge TCP est encore à compiler ; aucun nouveau binaire qualifié n'est annoncé.
- La preuve Pi réelle de `!s` en session host est confiée au worker de tests dans `real-shell-modes.integration.test.ts`. Les tests de routage mockés restent utiles mais ne remplacent pas cette preuve d'isolation.

### Qualification suivante et limites encore ouvertes

- Backend TCP : premier échange réel et test de réseau sortant réussis après rejeu et compilation. Snapshot `/tmp/pi-sandbox-qualified-241a2f/zerobox`, SHA-256 `241a2f7cad981ae123072086e58d1531bd1b20873c82666ba883654e90a84b39`, version fork.17. Ce jalon ne qualifie pas encore concurrence, protocole de contrôle, extinction ni révocation.
- La revue du coordinateur exige un acquittement de préparation avant exec, une terminaison du parent sans course sur PID, une limite de connexions réellement appliquée avant création des threads et un décodage commun des réponses et connexions asynchrones. Vérifie deux publications activées à des moments différents avec du trafic sur la première.
- Le worker de tests a exécuté la preuve réelle `!s` renforcée sur snapshot0522ff : 2 réussites, 42 assertions, 6,58 secondes. Elle couvre isolation et provenance puis retour à une commande hôte. Le test observe aussi les erreurs publiques du runtime Pi, depuis le démarrage jusqu’à la fermeture. Un essai du coordinateur pendant une refonte avait affiché des erreurs de hooks malgré un code de sortie nul : ne compte pas cet essai comme validation.
- Les fixtures de ces nouveaux essais injectent les leases et la récupération. Le préflight Analysis est simulé explicitement et ne constitue pas une preuve du moteur Analysis réel.
- Break-glass : les corrections de relecture et d’échéance progressent. La supervision par runtime ne suffit pas encore tant que l’activation d’un nouveau grant peut annuler le timer d’un runtime retraité. Conserve les échéances de tous les runtimes ayant porté l’exception.
- Intégration Pi des ressources et preuve réelle Unix/TCP en cours en parallèle. Le retrait dans la configuration doit aussi arrêter de façon bornée les accès déjà vivants. Un refus à la prochaine admission seul ne prouve pas cette révocation.

### Contrôles d’intégration complémentaires

- Le coordinateur confirme `local-resources.integration.test.ts` sur snapshot241a2f : 2 réussites, 47 assertions, 13,09 secondes. Socket précis et refus voisin, publication TCP paresseuse, commande concurrente, refus sortant et retrait à la prochaine admission sont couverts. Les échanges sont délimités par newline. La propagation EOF et la révocation des connexions vivantes ne sont pas démontrées par ce test.
- Le coordinateur a exécuté les 10 tests de migration : 46 assertions, aucun échec. Les deux groupes partagés command-execution et execution-provenance passent ensemble : 114 tests, 282 assertions.
- Le schéma enrichi des ressources passe Draft 2020-12 et quatre fixtures : global valide, projet restrictif valide, identité globale dans un projet refusée, publication UDP refusée.
- A6 moteur Analysis réel : le RED des modules manquants est corrigé par l’accord précis de la cible canonique de node_modules du runtime. Aucun paquet installé et aucun HOME ouvert. Le coordinateur confirme sur snapshot241a2f : 3 réussites, 23 assertions, 11,15 secondes, avec QuickJS/Python réels et processus Analysis extérieur. Les INPUTS sont immuables, l’accès hôte est refusé, HOME et tmp restent privés, la lease est nettoyée.
- Les corrections Pi de ressources sont encore en traitement : protection des sockets daemon connus/configurés et aliases, test A/B des exceptions temporaires et révocation active. Ne déduis pas leur validation des seuls tests d’échange réussis.

### Durées de vie et fermeture active

- Le test A/B avec vrais processus est corrigé : attends READY sur stdout avant chaque transition et conserve la promesse de fin dans un objet pour éviter son adoption par le helper async. Le spy des échéances ne remplace plus les autres timers. A1 et A2 terminent à l’échéance A, B reste actif puis termine à sa propre échéance. Le worker rapporte ensuite 32 tests lifecycle réussis, 147 assertions, incluant la révocation des ressources.
- La révocation active Unix/TCP a suivi un RED réel : après retrait puis admission suivante, les anciens processus continuaient d’accepter des connexions au-delà de huit secondes. Le correctif conserve la configuration des anciens services et interrompt leurs superviseurs quand une ressource a disparu du nouvel ensemble validé. Les runtimes sans retrait de ressource conservent le drainage ordinaire.
- Le worker confirme le fichier réel complet de ressources sur snapshot241a2f : 4 réussites, 83 assertions, 15,55 secondes. Cela couvre fermeture de l’écoute et du flux TCP maintenu, arrêt de l’ancien processus, fermeture des connexions Unix et absence de nouvelle connexion depuis cet ancien détenteur.
- Les anciennes fixtures safe-bash et les protections natives de configuration utilisent désormais des racines temporaires, un candidat explicite et un préflight Analysis contrôlé. Résultat du worker : 14 réussites, 5 non exécutés, 97 assertions. Sans candidat, skip explicite, aucun repli sur l’installation personnelle.
- Le coordinateur a exécuté `tsc --noEmit -p tsconfig.sandbox.json` avec succès. Cette configuration inclut les tests des frontières modifiées et évite le conflit de types de la commande isolée `--ignoreConfig`.
- La suite finale attend encore le backend stabilisé, son binaire release et la dernière isolation des anciennes intégrations client Analysis. La preuve de révocation Pi utilise l’arrêt de l’arbre de processus. L’API de révocation du SDK Rust n’est pas appelée par le CLI TypeScript.

### Reprise de la qualification backend et suite élargie

- Le backend a rapporté dix tests TCP réussis et une release SHA4854aaed417c08cc58b29839f2c9f81dd5eb83472ffeda90fc4fcf7fa493f338. Cette release n’est pas retenue comme finale : la réparation d’un rejeu avait aussi supprimé le refus d’un FD de socket Unix supprimé. Le worker l’a retrouvé et réintroduit dans le patch, mais son test ne se rejouait pas. Aucun succès de compilation après un hunk rejeté ne constitue une qualification.
- Le coordinateur a arrêté ce worker et transféré exclusivement le backend à worker_backend_finalize, GPT-5.6 Terra, avec contexte borné et les protections perdues explicitement recensées. Restaure production et tests Unix, puis vérifie le résultat sémantique du rejeu.
- La revue du monitor TCP a aussi trouvé qu’une seule écoute privée disparue fermait toutes les publications et arrêtait la surveillance. Corrige par publication : B doit rester joignable quand A s’arrête, puis A doit pouvoir se republier lors de son redémarrage dans la même commande. Préserve la révocation globale et le drainage borné.
- Première suite élargie Pi hors fichiers integration : 543 réussites, 15 échecs, 1711 assertions. Douze échecs de longueur Unix proviennent du TMPDIR `/tmp/.ctx-mode-*` imposé par l’outil de validation. Le coordinateur vérifie ces tests séparément avec le `/tmp` normal, sans modifier la limite production. Les trois autres échecs sont des fixtures anciennes : enabled dans le projet, absence du plafond host et allowUnsafeTarget:false dans une restriction Docker projet. Les workers les ont adaptées sans retirer leurs assertions métier.
- Les anciens tests client Analysis injectent désormais le vrai moteur avec racines temporaires. Un cas additionnel utilisant le client IPC par défaut est écrit et typé, avec HOME jetable et binaire lié dans ce HOME. Il attend l’empreinte et la provenance du binaire final.

### Suite Pi corrigée

- Le coordinateur confirme les deux fichiers de leases et backend avec TMPDIR normal : 16 réussites, 173 assertions. Le refus des chemins réellement trop longs reste testé.
- Rejeu des 54 fichiers de tests des frontières Pi, hors fichiers nommés integration, avec TMPDIR=/tmp et snapshot241a2f explicite : 558 réussites, zéro échec, 1873 assertions, 46,89 secondes. Les tests réels contenus dans execution.test.ts utilisent désormais la factory candidate et des fixtures isolées.
- Les contrôles finaux restants portent sur le backend restauré, les intégrations contre sa release, la provenance exacte, le cas IPC Analysis par défaut et la clôture documentaire. Ne rejoue pas les unités inchangées uniquement pour augmenter leur compteur.

### Qualification finale en cours

- Le second worker prépare la suite de frontière complète, intégrations comprises. La passe de 558 tests couvre les fichiers hors suffixe `.integration.test.ts`, pas cette suite complète.
- L’audit complémentaire retrouve des fixtures historiques dépendant du binaire, des leases et du rollback personnels. Leur adaptation utilise des chemins de candidat explicites et des autorités de test isolées. Le contrat de dépendances sera associé au manifeste du build local, sans le présenter comme une release taguée.
- Le backend reproduit un défaut réel : l’arrêt d’un serveur publié ferme aussi une autre publication. Le correctif doit gérer la fermeture et la republication indépendamment pour chaque serveur, avec drainage des connexions de la génération précédente.
- Le rejeu Unix est à nouveau propre et conserve le contrôle d’un descripteur dont le socket a été supprimé. Le binaire final reste à construire et qualifier après stabilisation du correctif TCP.
- Le test A/B de publication indépendante passe. La revue du coordinateur retrouve un second cas : deux arrêts rapides remplacent une échéance de drainage par une autre. Conserve toutes les générations retirées et traite les échéances même lors du rejet d’une connexion ou d’un quota atteint. Ne lance aucune nouvelle connexion après observation de la révocation globale.
- Le diagnostic Docker construisait une configuration vide et ignorait l’autorisation explicite du binaire CLI canonique, ce qui bloque notamment son emplacement WSL. Le second worker transmet la configuration effective au diagnostic du broker, en gardant le grant diagnostic limité à `ps`, et vérifie le parcours réel avec fixtures.
- Première suite backend complète en release : 129 réussites, 39 échecs, 4 ignorés. Les erreurs communes de `bwrap` sur le cwd sont en diagnostic. Ce résultat ne qualifie pas la release. Conserve le refus d’un cwd inaccessible et corrige les anciennes fixtures uniquement si elles dépendaient du repli supprimé vers `/`.
- Limite TDD complémentaire : le RED TCP initial prouvait que l’arrêt de A déconnectait B. Le scénario de deux redémarrages avec vieux flux a été ajouté avec son correctif, sans RED séparé observé. La course d’inscription d’un relay après expiration de sa génération est traitée avant la qualification finale.
- Le diagnostic Docker corrigé conserve la configuration effective et le `this` des dépendances injectées. RED réel puis GREEN avec le broker de fixture. Les deux fichiers production sont formatés et les deux typechecks passent.
- Limite de lancement groupé Pi sous Bun 1.3.14 : un groupe contenant les imports Docker puis les tests Linux/isolation a produit `EEXIST: epoll_ctl` dans `internal:fs/streams` → `internal:util/colors` → `node:assert` → `signal-exit`, puis une erreur sur `process.stderr.fd` dans Jiti. Aucun test Docker n’était exécuté dans le reproducteur réduit. Les contrôles instrumentés observent des shutdowns résolus, fd 1/2 inchangés, aucun enfant et aucune lease restante. Les passes instrumentées avec et sans `--isolate` réussissent toutes deux (11 tests, 113 assertions). Cause non attribuée, instrumentation retirée, aucun correctif de dépendance. Qualifie la frontière fichier par fichier dans des processus distincts et ne présente pas cette preuve comme une passe groupée réussie.
- Concurrence backend : deux CLI indépendants avec homes, racines et ports distincts servent simultanément trois échanges chacun (test réel réussi). Le groupe de onze fixtures TCP concurrentes a produit sept échecs de bridge, y compris après isolation de `ZEROBOX_HOME`. La cause reste en diagnostic. Leur sérialisation externe donne onze réussites mais ne remplace pas cette preuve manquante. Les scénarios de connexions multiples, conflit entre deux commandes et republication à l’intérieur d’une fixture restent concurrents.
- Environnement du contrôle final : `rustc 1.96.0 (ac68faa20 2026-05-25)`, noyau `Linux 6.18.33.2-microsoft-standard-WSL2`.
- Recontrôle Pi par le coordinateur après le correctif du diagnostic Docker : `index.test.ts` et `docker-access.test.ts` réussissent ensemble (46 tests, 102 assertions). `bun run typecheck` et `./node_modules/.bin/tsc --noEmit -p tsconfig.sandbox.json` réussissent sur le code stabilisé.
- Reprise TCP après correction F1 : RED déterministe sur une révocation pendant l’attente BIND/UNBIND, puis GREEN avec reconnaissance de la révocation et drainage. Le groupe de douze tests TCP passe désormais sans verrou externe (12 réussites, 161 filtrés, 10,31 s), y compris deux CLI indépendantes et les scénarios SDK. La qualification finale doit employer le CLI release identifié, en distinguant les tests SDK liés au profil de compilation des tests.

## Espace de travail

- Pi : `~/projects/pi-integrations/.worktrees/pi-sandbox-generalisation`, branche `codex/sandbox-generalisation`, base `59b24f5`.
- Zerobox : `~/projects/shared-services/sandboxes/zerobox-generalisation`, branche `codex/sandbox-generalisation`, base `5c53089`. Réutilise le cache `target` existant, sans le nettoyer.
- Réutilise les dépendances installées via un lien `agent/node_modules`. Ne modifie pas leur contenu. Vérifie le package réellement résolu lors des essais Pi.
- Préserve l’installation personnelle, ses configurations, services et changements préexistants. Ne publie, ne fusionne et n’active pas personnellement le candidat.

## Ordre et dépendances

| Lots | Interface partagée | Ordre retenu |
| --- | --- | --- |
| A1 / A3 | Politique de lecture et socle système | Worker principal : qualifier puis corriger avec RED/GREEN. |
| A2 / A3 / A4 | Configuration effective, types, routage shell | Stabilise le contrat avant de déléguer les consommateurs. |
| A1b / A2 | Ressources génériques Zerobox et compilation Pi | Qualifie le backend séparément, puis intègre son contrat. |
| A4 / A5 | Routage, provenance et durée de vie | Vérifie les invariants après changement du contrat. |
| A6 | Documentation et preuves de tous les lots | Termine après intégration, sans attribuer les résultats anciens au candidat. |
| A7 | UDP | Hors première livraison, selon Q7. |

Décision de coordination : conserve les contrôles de conformité localement. N’ajoute pas de sous-agent de revue ou de sécurité. Ne confonds pas revue locale du résultat et autorisation d’activation personnelle.

## Progression

### État courant après clôture du socle

- A1/A3, sous-lot socle de lecture/PATH : rapport final reçu et relu. Pi ciblé : 26 réussites, 181 assertions. Pi réel avec candidat vérifié sous WSL : 1 réussite. Les configurations global/projet et le plafond tmp restent dépendants d’A2.
- A1b, prérequis montages/FUSE/denyWrite : terminé et relu. `private_tmp_` : 4 réussites. Tests réels FUSE, denyWrite-only, descendants avec parent lisible/non lisible et denyRead parent : réussis. Ordre des mounts, replay, formatage Rust et diff : réussis.
- Binaire candidat de ce jalon : `~/projects/shared-services/sandboxes/zerobox-generalisation/target/debug/zerobox`, version `0.3.3-fork.17`, SHA-256 `9519c2d7cc42960067c4c7bacf6499b20d3435c4276525f41cd72761e9ae6a11`. L’installation personnelle reste inchangée. Linux natif non exécuté.
- A2 : attribué au worker principal Terra après réception de son rapport final. Exécute le brief `2026-09-10-sandbox-generalisation-A2-brief.md`, d’abord format/loader/résolution, puis commandes/migration et wiring actif.
- A2, premier jalon : RED public `loadSandboxConfig` sur le Docker intégré autrefois réservé à sandbox.global.json, puis GREEN ciblé du nouveau chargement global versionné + projet. Mode `sandbox|host` distinct du profil descriptif `default|custom|host`. Revue intermédiaire transmise : retire les réexports/écrivains historiques actifs, exclue isolated/integrated du type actif, canonicalise les liens avant plafonnement et distingue fichier absent de fichier invalide. Docker doit aussi accepter une restriction projet des opérations/cibles, pas seulement enabled.
- A2, jalon suivant du worker : héritage/listes vides/exclusions et cinq cas Docker annoncés verts. La revue maintient ouverts les booléens absents dans une section présente, la désactivation globale sans effacement du plafond inactif et les exceptions sensibles réservées au global. Migration encore prototype : trois tests verts de preview/cancel/archives ne valident pas M1–M5. Exige les archives de tous les fichiers modifiés, les règles historiques global/projet/settings, la validation réelle des destinations, la vérification d’empreintes avant publication et un échec injecté à la vraie frontière entre les deux publications. Retire le drapeau public artificiel failBeforePublish.
- A2, rapport final partiel reçu puis lot réattribué : 49 tests unitaires ciblés annoncés verts, mais migration compatible et choix UI incomplets. Des `@ts-nocheck` ont été ajoutés aux tests historiques : exige leur retrait et l’adaptation des fixtures au vrai harness, sans accepter le typecheck obtenu ainsi. Exige aussi conservation de docker.targets lors des bascules on/off et reconstruction drainée du runtime après changement de configuration.
- A2, reprise harness : les tests réels utilisent encore le binaire personnel ancien et échouent sur le montage privé. Injecte explicitement le candidat qualifié et sa provenance dans un véritable entrypoint de test; ne remplace pas l’installation et n’ajoute pas de bypass production. Un producteur interne fabrique encore hostCapability: undefined : supprime le champ du contrat actif au lieu d’affaiblir les guards qui refusent toute présence legacy explicite, y compris undefined.
- A2, preuve harness suivante : safe_bash réel avec le candidat SHA `9519c2d7…` annoncé à 9 réussites, 5 tests opt-in non exécutés, 42 assertions, aucun échec. Le fixture Bun déclare séparément PATH et allowRead. Migration et reconstruction drainée restent ouvertes. La revue demande une comparaison normalisée avec le socle pour dériver custom, couvrant aussi les restrictions seules et le retour au socle.
- A2, révision suivante : résolution/profil à 25 réussites, avec listes vides jusqu’à la compilation et wildcard enfant sous plafond. Le nouveau rerun harness révèle 3 échecs réels (shebang, écriture Git, Bun) après conservation des denies du socle. La preuve précédente de 9 réussites n’est donc pas attribuable à cette révision. Compare les politiques compilées et les fixtures avant toute modification du backend, qui reste gelé. Migration à 4 tests annoncés verts, interface de choix explicite et preuve de drainage encore ouvertes.
- A4 : retrait de routage et refus safe_bash intégrés, validations ciblées vertes. Types, provenance, retrait complet des adaptateurs et preuve harness builtin restent à achever avec A2/A4.
- A4, parallèle borné : Spark indisponible, confie la preuve runtime builtin à un worker Terra supplémentaire dans le seul nouveau fichier `bash-execution/legacy-builtin-payload.integration.test.ts`. Réserve ce chemin au worker de test. Le worker principal conserve tous les autres fichiers de production et de tests. Attends le rapport final avant de considérer cette preuve acquise.
- A4, preuve builtin reçue et exécutée par le coordinateur : 1 réussite, 24 assertions, aucun échec. Le vrai module reçoit les champs legacy string/null/undefined, produit le diagnostic précis et ne crée aucun processus; un contrôle positif ordinaire crée son marqueur. Harness 0.7.0 / Pi 0.85.0 résolus. Avec `propagateErrors:false`, le harness convertit le throw en résultat et son événement porte isError:false : la preuve porte sur le diagnostic et les marqueurs, pas sur ce drapeau de collecte.
- A2/A5, revue de couverture : les 73 réussites ciblées et deux typechecks annoncés ne suffisent pas. Le remplacement de lifecycle.test.ts a supprimé des invariants runtime encore requis. Confie leur restauration adaptée au second worker, propriétaire exclusif de ce fichier. Le principal conserve la production et la réparation FUSE. Aucun lot A2/A5 complet n’est déclaré.
- A2, reprise migration requise après FUSE : la validation des temporaires ne résout encore que les documents et Docker, sans compilation de politique shell. Le marker ne conserve pas assez d’état de reprise, et cancelIncompleteMigration l’efface sans vérifier/restaurer une publication partielle. Corrige ces deux frontières et couvre une interruption réelle entre publications avant de déclarer M1–M5 complets.
- Diagnostic des 3 échecs : trois reproductions directes Zerobox annoncées vertes, incluant deny exact/globs, tmp privé et vue WSL réelle. Aucun défaut backend établi, hash `9519c2d7…` conservé. Poursuis dans Pi en comparant la politique et l’argv réellement transmis, puis corrige la différence prouvée. Ne modifie pas le patch backend sur la seule hypothèse FUSE.
- A5, couverture restaurée reçue, relue et exécutée par le coordinateur : 16 réussites, 56 assertions. Les handlers et le chargeur réels lisent global/projet v2, les services backend sont mockés. Retries Analysis, cleanup, drainage, générations concurrentes, shutdown/reload et portée session sont couverts. La révocation par édition externe directe reste à démontrer en A5.
- A2, parallèle migration : réserve `capabilities/migration.ts` et `migration.test.ts` au second worker pour achever validation effective et reprise vérifiée. Le principal garde `index.ts`, le câblage UI et le diagnostic Pi. Le fichier lifecycle restauré est figé pendant ces deux sous-lots.
- Diagnostic Pi, différence observée : l’argv capturé transmet le bon `-C <fixture>`, mais la cible semble démarrer à `/`. Vérifie `/bin/pwd -P` et un fichier relatif, puis ajoute au reproducer direct le deny du parent technique `<lease-parent>/zbx` (enfants lease home/tmp accordés), différence absente des premiers tests. Ne réouvre pas ce parent et ne tolère aucun fallback silencieux de cwd. Cette observation reste une piste jusqu’à reproduction.
- Cause cwd reproduite : le test direct enrichi échoue réellement sur `/bin/pwd -P == projet`. Le constructeur bwrap n’émet `--chdir` que si la canonicalisation change le chemin; avec ces montages, cwd devient `/`. Correctif générique autorisé : impose toujours le cwd canonique après montages, couvre le refus de cwd sans exécution, rejoue le patch puis revalide Pi avec un nouveau hash. Le GREEN final de cette correction reste attendu.
- Après ajout de chdir, le reproducer échoue explicitement sur le passage vers le projet : une vue FUSE sur HOME cache un ancêtre structurel du descendant accordé. Le principal traite ce second défaut sans ouvrir les parents ni les voisins; préserve notamment denyRead parent + enfant accordé refusé.
- Migration core corrigée, rapport final reçu et tests exécutés par le coordinateur : 9 réussites, 43 assertions. Récupération d’une destination déjà identique avant publication, restauration Buffer exacte et fsync fichier/répertoire inclus. Le socle partagé réside désormais dans `runtime/default-config.ts`, utilisé par loader et migration. Le câblage UI de reprise reste ouvert.
- A2, validation imbriquée : réserve `capabilities/authority.ts`, `policy.ts` et leurs tests au second worker. Refuse les clés imbriquées inconnues avant composition et conserve les réglages environnement/réseau reconnus sous plafond. Les validateurs actuels peuvent encore ignorer silencieusement certains champs; la migration hérite de cette limite jusqu’à correction du propriétaire commun.
- A2, validation imbriquée terminée et exécutée par le coordinateur : 26 réussites, 79 assertions sur profiles/authority/active-authority/migration. Les clés inconnues sont refusées; environnement et allowLocalBinding sont conservés sous plafond. Le second worker traite désormais UI migration/recovery et reconfiguration directe dans index/lifecycle/runtime capabilities/builtin/routing; le principal reste sur le reproducer backend indépendant.
- F4 Bun : `bun -e` réussit, `bun run root-probe` échoue sur CouldntReadCurrentDirectory. La recherche du coordinateur trouve le [signalement Bun 28220](https://github.com/oven-sh/bun/issues/28220), décrivant getcwd réussi puis openat des ancêtres refusé, ainsi qu’un [signalement Bun 1.3.14](https://github.com/oven-sh/bun/issues/30859). Confirme avec une fixture hôte à ancêtres non lisibles avant d’attribuer la limite au runtime Bun. N’ouvre pas les parents et ne modifie pas le Bun global pour masquer cette incompatibilité.
- F4, contrôle hôte initial annoncé vert : aucune attribution à Bun seul n’est acquise. La [source Bun 1.3.14 run_command](https://github.com/oven-sh/bun/blob/bun-v1.3.14/src/cli/run_command.zig#L794) convertit readDirInfo null en cette erreur; le [resolver du même tag](https://github.com/oven-sh/bun/blob/bun-v1.3.14/src/resolver/resolver.zig#L2839) ouvre tous les parents et retourne null sur erreur. Vérifie avec le même binaire absolu et un opendir du parent effectivement refusé que le contrôle hôte est non vacuant. La fermeture du ticket par PR33119 ne constitue pas une version corrective vérifiée (PR intitulée AppContainer Windows).
- A1b sockets/publication, A5 et A6 : restent à réaliser. A7 UDP reste reporté.

### Historique des jalons et incidents

- A1/A3 : en cours, worker principal GPT-5.6 Terra. Contrat coordonné : `environment.path` règle le PATH sans octroyer de lecture.
- A1/A3 : rapport de code reçu et récupéré après incident. Le contrôleur a trouvé un test runtime non enregistré (`test.skipIf` mal appelé). Correction confiée au même worker et preuve désormais confirmée : 1 test réellement exécuté, 1 échec sur le montage privé. La validation du lot reste en attente du backend.
- A1b, prérequis du socle : correction générique des montages privés confiée au worker principal dans le worktree Zerobox dédié. Sockets et publication ne sont pas encore implémentés.
- A1b, dernier état rapporté : replay complet réussi avec le patch final. Le test Rust atteint désormais le target mais échoue sur sa vérification FD. Distingue fuite et indisponibilité de `/proc/$$/fd` par des diagnostics précis avant de modifier la fermeture. Aucun GREEN runtime du candidat n’est encore acquis.
- A1b, adaptation dépendante : autorise aussi la mise à jour des contextes/signatures du patch `upstream-docker-broker-hidden-route.patch` rendus obsolètes par les descripteurs préouverts. Préserve sa logique proxy/Docker et rejoue toute la chaîne avant compilation. Les essais de replay rejetés ne constituent pas une validation.
- Décision ultérieure A1b : conserve finalement les patches historiques inchangés et ajoute `upstream-private-bind-fd.patch` en fin de chaîne avant formatage. Cette organisation évite une cascade d’ajustements de contextes dans les patches Docker et de diagnostics. Restaure uniquement les modifications du worker sur les anciens patches concernés, puis valide le rejeu complet et les assertions historiques adaptées.
- A4, sous-lot schéma : quick worker a retiré `hostCapability`, RED constaté puis 31 tests réussis. Le contrôle du coordinateur a trouvé une assertion non typable et le consommateur safe_bash restant à adapter. Correction et refus des anciens appels confiés au quick worker.
- A2, A1b, A4, A5, A6 : à réaliser.
- A7 : différé par décision utilisateur.

### Coordination du retrait des routes

Le nouveau quick worker travaille dans `~/projects/pi-integrations/.worktrees/pi-sandbox-routing`, branche `codex/sandbox-generalisation-routing`, sur `builtin-bash.ts` et `capability-routing.test.ts` seulement. Le premier rapport annonce 10 tests réussis. La revue locale demande encore la conservation de la révocation hôte, le refus sans repli après une erreur du sandbox et un diagnostic de migration qui ne recommande pas automatiquement le mode hôte. Attends son rapport final corrigé avant intégration.

Rapport corrigé reçu et deux fichiers intégrés au candidat principal. Le contrôleur a exécuté les 10 tests (37 assertions, aucun échec), `tsc --noEmit -p tsconfig.sandbox.json` et `git diff --check` dans le worktree isolé : tous réussis. Les deux révocations sont conservées, les erreurs backend ne changent pas de backend, et le diagnostic n’impose pas le mode hôte. La preuve du guard de `registerBuiltinBash.execute` via le vrai harness reste à ajouter en A4. Les types legacy et la provenance restent à migrer.

### Correction du rejeu des montages privés

Le test Rust réel a atteint une réussite avec inspection contrôlée de `/proc/$$/fd`, mais le test unitaire adjacent a révélé un ordre d’arguments incorrect. La régénération du patch a ensuite échoué parce que la base avait été capturée après le formatage final de `sync.sh`, alors que le patch s’applique avant ce formatage. Régénère depuis les octets au point exact d’application. Les patches historiques restent identiques à HEAD. Ne compile pas les sources après un rejet et ne considère pas la réussite antérieure comme validation de la révision suivante.

Dernier résultat du worker : replay complet réussi, 1 test unitaire réussi et groupe `private_tmp_` à 3 réussites. Le montage et sa destination sont insérés après la construction des montages et avant `--remount-ro /` pour une racine tmpfs. Une insertion au début de l’argv était masquée par le `--tmpfs /` ultérieur. Le contrôleur a relu le patch et les assertions de refus de lecture et d’inspection FD. Build candidate et preuve réelle Pi encore en cours.

Qualification Pi suivante : 1 test exécuté, 1 échec `invalid private bind mount: Permission denied` au moment de l’ouverture du FD. Le chemin Pi ajoute des exclusions dynamiques et donc des montages FUSE avant le montage tmp. Identifie la source exacte refusée avant de déplacer la préparation dans le parent. L’ouverture `O_RDONLY | O_DIRECTORY` déclenche `dynamic_fs.rs::opendir`, qui vérifie la lecture de la racine de vue. Qualifie un handle de chemin sans droit de lecture si cette cause est confirmée. N’affaiblis pas le filtre FUSE.

Diagnostic confirmé : `view-0` est refusé, pas le répertoire tmp. `O_PATH` contourne la demande d’ouverture en lecture sans changer les autorisations de la vue. Le `source.metadata()` suivant déclenche encore `getattr → open_metadata_checked → check_read`; retire ce contrôle redondant uniquement si les tests confirment que `O_DIRECTORY | O_NOFOLLOW` rejette toujours fichiers et liens. Le bind réel reste à qualifier. Ne déplace pas les FD dans le parent sur la base de ce seul incident. Une source technique explicitement autorisée par la politique Pi peut rester visible sous son chemin d’origine; ne confonds pas cet accès avec une fuite du parent de contrôle ou du `/tmp` hôte.

Après retrait du fstat redondant, bwrap demande à son tour le type de la source et échoue sur son getattr. Décision D10 locale : fournis un attribut de répertoire synthétique uniquement pour l’inode racine d’une vue FUSE dont la lecture est refusée par la politique. Vérifie que le constructeur garantit une racine répertoire. N’expose ni taille ni dates ni contenu réels, ne modifie pas les contrôles des enfants, d’ouverture, de liste ou de lecture, et ne masque pas les autres erreurs. Exige une preuve réelle de montage avec descendant accordé lisible, voisin et liste racine refusés, attributs synthétiques stables. Cette adaptation ne constitue pas une ouverture de lecture hôte.

Dernière preuve backend rapportée : test FUSE réel RED puis GREEN (1 réussite), groupe `private_tmp_` à 4 réussites, unité ordre argv à 1 réussite. Le contrôleur a relu l’adaptation synthétique et son test. Le candidat identifié par SHA `195ffef7eb0e60f19d7a5f9b526d53a5844bba77ec5ee6ed807fa1aea6e07e10` atteint désormais la commande du test Pi. Ajuste l’assertion WSL : la présence d’un `/mnt/c` synthétique vide n’est pas un accès aux fichiers Windows. Vérifie un chemin hôte existant et son absence dans le sandbox pour une preuve non vacuante.

La vérification non vacuante montre que `/mnt/c/Users` reste visible. Piste source identifiée par le contrôleur : `sandbox.rs::build_fs_policy` transforme chaque `deny_write` en entrée `Read` inconditionnelle, ce qui peut accorder une lecture absente du socle. Confirme par un fichier de fixture extérieur sans allowRead, puis corrige la compilation générique sans règle spéciale Windows. Un denyWrite ne doit jamais réouvrir une lecture absente ou refusée. Préserve aussi les restrictions d’écriture sur un descendant accordé.

Le worker rapporte ensuite RED/GREEN du reproducer denyWrite-only et GREEN du test Pi réel (1 réussite), avec SHA candidat `03524a0692fcfd19b0e91b1ca83a676c803d7fc38b24753347b40a70ff477cbc`. Revue encore ouverte : le premier correctif saute un denyWrite si son parent n’est pas lisible, même si un enfant est explicitement writable. Couvre ce cas et conserve le refus d’écriture sur l’enfant sans ouvrir le parent. Couvre aussi denyRead parent + denyWrite enfant et les write-globs réels avant le rapport final du lot.

La première variante descendant allowWrite est ensuite RED puis GREEN, avec denyRead parent préservé, write-glob FUSE vérifié et test Pi réel réussi. SHA intermédiaire : `de4d7981387c700f4f5f377eb7bf0b478e286e9494eaf3ff331adefcf0d77f42`. La revue demande de couvrir aussi le parent déjà lisible : collecte et rétrograde les accords Write descendants dans les deux variantes, pas seulement lorsque le parent n’est pas lisible. Attends les résultats de cette dernière révision avant de figer le hash final.

À couvrir lors des ouvertures réseau P2 : le même compilateur ajoute actuellement `/run` entier aux lectures lorsque `net_enabled`. Vérifie le besoin réel et la possibilité de limiter aux ressources techniques exactes avant de déclarer les ouvertures réseau bornées. Cette observation source ne constitue pas encore une preuve runtime ni un correctif.

Le quick worker a atteint sa limite d’usage Spark avant d’écrire le test harness builtin supplémentaire. Aucun nouveau fichier n’a été produit. Son lot de deux fichiers terminé reste validé; conserve la preuve runtime builtin parmi les travaux A4 restants.

## Contrôles exécutés par le coordinateur

- Dans le worktree Zerobox dédié, `./scripts/sync.sh` a rejoué les patches existants jusqu’au succès, sur la révision amont épinglée `9b8cf56cdefb09f54564ccc295fd42f6647f558f`. Aucun hunk rejeté. Le checkout reste propre après cette préparation.

- `bun test --isolate extensions/_shared/sandbox-runtime/` : 32 réussites, 0 échec, avant les changements du registre et de la révocation.
- `bun test --isolate extensions/_shared/command-execution/exec.test.ts extensions/bash-execution/safe-bash/execution.test.ts` : 42 réussites, 0 échec après le sous-lot schéma/refus safe_bash.
- `bun test --isolate extensions/bash-execution/capabilities.integration.test.ts -t 'rejects legacy safe_bash'` : 1 réussite, 0 échec, 11 filtrés. Ancien appel refusé dans le vrai runtime Pi, marqueur absent.
- Après intégration du routage : `bun test --isolate extensions/bash-execution/capability-routing.test.ts extensions/_shared/command-execution/exec.test.ts extensions/bash-execution/safe-bash/execution.test.ts` : 52 réussites, 0 échec, 123 assertions.
- Après les deux corrections de defaults `environment.path` : `bun run typecheck` et `./node_modules/.bin/tsc --noEmit -p tsconfig.sandbox.json` réussissent tous deux sur le candidat principal. Ces résultats couvrent le routage intégré et les tests backend adaptés au socle.
- Résolution observée depuis le worktree : Pi utilise `~/projects/pi-core/packages/coding-agent/dist/index.js`, le harness utilise `~/.pi/agent/node_modules/@abdwhb-png/pi-test-harness/index.js`. Les tests chargent les extensions candidates depuis le worktree.
- Reste à corriger la guidance « integration commands » et à achever les autres frontières d’entrée et routes A4. Les succès ci-dessus ne valident pas encore toute la migration.

## Limites des preuves

RED backend supplémentaire : `cargo test --offline --locked -p zerobox private_tmp_uses_preopened_source_when_lease_parent_is_denied -- --nocapture` exécute 1 test et échoue avant target sur `bwrap: Can't mkdir /tmp: Read-only file system`. Traite aussi la préparation de la destination du montage sous racine restreinte. Ne confonds pas cette preuve Rust avec le RED Pi sur `symlink_metadata` : les deux parcours doivent passer après correction.

À réexaminer en A2/A3 : le candidat autorise un chemin Windows précis en lecture mais garde `/mnt/c` interdit en écriture. Cette limite actuelle n’est pas une preuve d’impossibilité de l’écriture ciblée sous WSL. Distingue toujours accès fichiers, session hôte et exécution Windows.

Qualification A1/A3 : le socle explicite déclenche `invalid private bind mount: Permission denied` avant le processus cible. Le worker a identifié `symlink_metadata` du répertoire source de `/tmp` après restrictions dans `inject_private_bind_mounts`. Une expérience avec `/` confirme que le montage démarre mais expose un fichier voisin interdit. Conserve le socle restreint et corrige le backend avant de déclarer A1/A3 validé. Le test réel reste une preuve d’échec jusqu’au candidat corrigé.

Incident de coordination : le quick worker a restauré `policies.ts` et `policies.test.ts`, puis supprimé les nouveaux `shell-baseline*` et les plans copiés, hors périmètre autorisé et sans sauvegarde. Il l’a confirmé dans son rapport final. Le contrôleur l’a interrompu. Le worker principal récupère ses changements depuis son contexte et dispose seul du worktree pour la production. Les documents sont récupérés depuis la copie intacte dans l’installation personnelle. Ne compte pas la qualification interrompue comme achevée.

L’environnement disponible est WSL. Une validation Linux native reste à obtenir ou à signaler comme manquante. Ne la remplace pas par des fixtures sous WSL.

Les premiers typechecks du worktree ont révélé des dépendances locales absentes du nouveau checkout. Réutilise aussi les installations existantes de sandbox, markdown-links, session-cron et agent/npm via des liens locaux. Aucune dépendance installée ou mise à jour. Après ces liens, les erreurs observées se limitent aux deux points du sous-lot schéma transmis au quick worker.
