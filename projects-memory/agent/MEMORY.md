Pi agent directory has mixed package versions: @mariozechner/* at v0.66.1 and @earendil-works/* at v0.79.0 coexist in package.json. A tuned fallow config (.fallowrc.json) was set up with ignoreDependencies covering pi runtime packages and ESM-only @earendil-works packages. A "fallow" role was created at ~/.pi/agent/roles/fallow.md for running fallow analysis. <!-- created=2026-06-09, last=2026-06-09 -->
§
This pi agent repo (/home/abdwhb/.pi/agent) uses an HTTPS remote (https://github.com/abdwhb-png/my-pi-setup) and push fails because credentials aren't available in the environment. User needs to push from terminal. <!-- created=2026-06-10, last=2026-06-10 -->
§
The `yeet` extension should default to add+commit only (no push). Push is opt-in via args like `/yeet push`. Do not include --force push by default. <!-- created=2026-06-10, last=2026-06-10 -->
§
Parallel subagent delegation with forked context causes massive token waste. Real case: 4 workers doing simple tasks (export 5 functions, convert 2 test files, create config) consumed 1.3M–1.5M tokens each over 18–28 min. Root cause: forked context inherited the full parent conversation (vitest research, plan iterations, corrections) — every turn carried ~30K irrelevant tokens. Fix: use `defaultContext: 'fresh'` on task-doer agent + follow safe-delegation skill. Fresh context = subagent sees only its task, no inherited plan context. <!-- created=2026-06-10, last=2026-06-10 -->
§
Worker model changed from `or/poolside/laguna-m.1:free` (200K context) to `or/qwen/qwen3.6-plus-preview:free` (1M context, free). task-doer agent also uses qwen3.6-plus with same 1M context. Fallbacks: or/deepseek/deepseek-v4-flash, or/google/gemma-4-26b-a4b-it. The `or/` prefix is mandatory for ALL subagent models — routes through custom proxy provider to ensure OpenRouter routing (prevents native provider auth errors). <!-- created=2026-06-10, last=2026-06-10 -->
§
LLM subagents default to copy-pasting source functions into test files instead of importing them — even when the parent AGENTS.md explicitly prohibits this. The vitest skill says "copying is OK when imports fail due to jiti" and the LLM latched onto that excuse. Must explicitly instruct subagents: tests MUST import the real module; never duplicate source code. Include this in task-doer's system prompt or task instructions. <!-- created=2026-06-10, last=2026-06-10 -->